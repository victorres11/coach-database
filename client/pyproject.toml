[project]
name = "coachdb-client"
version = "0.1.0"
description = "Python client for the Coach DB API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


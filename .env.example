# Coach Database Environment Variables

# Webhook API Key (for automated staff updates)
# This key is used by external services (Pipedream, monitoring scripts) to authenticate
# webhook requests to POST /api/webhooks/staff-update
WEBHOOK_API_KEY=your-webhook-api-key-here

# OpenAI API Key (for staff monitor script)
# Used to extract structured data from news articles
OPENAI_API_KEY=your-openai-api-key-here

# Brave Search API Key (optional, for staff monitor script)
# Used to discover recent coaching changes via search
BRAVE_API_KEY=your-brave-api-key-here

# Database
DATABASE_URL=./db/coaches.db

# CollegePressbox Cookie (for scraping)
COLLEGEPRESSBOX_COOKIE=your-cookie-here
